<div class="container">
  <div class="row">
    <div class="col-md-4">
      <br>
      <br>
      <h1>{{#link-to "artists.new"}}New Artist{{/link-to}}</h1>
      <br>
      <br>
      {{#each model.artists as |artist|}}
        <div>
          {{#link-to 'artists.artist' artist.id}}
            {{artist.name}}
          {{/link-to}}
        </div>
      {{/each}}
    </div>
    <div class="col-md-8">
      {{outlet}}
    </div>
  </div>
</div>
